spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/anime_reco
    username: postgres
    password: 1124
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

server:
  port: 8083

app:
  jwt:
    secret: change-this-secret-key-change-this-secret-key
    expiration-ms: 86400000